<Window x:Class="NeoTUN.Windows.Dialogs.ImportProfileDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Import Profile" Height="400" Width="600"
        Background="#FF1E1E1E" WindowStartupLocation="CenterOwner">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Import Profile from URI" 
                   Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>
        
        <TextBlock Grid.Row="1" Text="Paste your profile URI below:" 
                   Foreground="White" Margin="0,0,0,10"/>
        
        <TextBox Grid.Row="2" x:Name="UriTextBox" 
                 Background="#FF2D2D30" Foreground="White" 
                 BorderBrush="#FF3F3F46" BorderThickness="1"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 Padding="10" FontFamily="Consolas"/>
        
        <TextBlock Grid.Row="3" x:Name="StatusTextBlock" 
                   Foreground="Orange" Margin="0,10,0,0" 
                   Visibility="Collapsed"/>
        
        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Import" x:Name="ImportButton"
                    Background="#FF4CAF50" Foreground="White"
                    Padding="20,8" Margin="0,0,10,0"
                    Click="ImportButton_Click"/>
            <Button Content="Cancel" x:Name="CancelButton"
                    Background="#FF6C6C6C" Foreground="White"
                    Padding="20,8" IsCancel="True"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>